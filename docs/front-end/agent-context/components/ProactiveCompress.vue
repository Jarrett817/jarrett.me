<template>
  <div class="mt-3 rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-left">
    <p class="text-[0.72rem] leading-snug text-gray-700">
      工具返回过长时，在写入 <code>messages</code> 前先做摘要压缩（例如 2000 token → 约 200 token），避免被动触发窗口压缩。
    </p>
  </div>
</template>
