---
description: Agent 幻灯片图示组件（静态 Tailwind）
globs:
  - docs/**/agent-*/**
  - docs/**/components/Context*.vue
  - docs/**/components/Compaction*.vue
  - docs/**/components/Memory*.vue
  - docs/**/components/ReadFile*.vue
  - docs/**/components/Coherence*.vue
alwaysApply: false
---

# Agent 幻灯片图示（Vue）

## 分工

- **唯一内容源** → `article-sections.vue`（阅读 `mode="read"` / 幻灯片 `mode="slide"`）
- **阅读入口** → `index.md` 直接 `<ArticleSections mode="read" />`
- **幻灯片入口** → `interactive-slides.ts` 加载同一文件，`slides-viewer` 传 `mode="slide"`
- **图示组件** → `components/*`（Markdown 不好画的图；适合演示的可做交互）

## 交互约定

- 根节点：`data-reveal-interactive` + `@click.stop`（避免触发 Reveal 翻页）
- 主题：`jarrett-theme.scss` 对 `.reveal-vue-deck` 恢复 `pointer-events: auto`
- 翻页用箭头 / fragment；页内探索用组件点击或滑块

## 样式

- Tailwind 直接写在模板，不用 `commom.scss` 抽组件类
- 禁止 `<style scoped lang="scss">`

## `article-sections.vue`

唯一正文：多根 `<section>`；`mode="slide"` 时加 `fragment` 步进。
