---
description: Agent 幻灯片：正文只写概念，实现佐证单独放讲稿
globs:
  - docs/**/agent-*/**
alwaysApply: false
---

# Agent 幻灯片内容分工

## 幻灯片（Reveal）

- 内容写在 **`article-sections.vue`**：一页一观点（speaker-led），塞不下就拆 `<section>`
- 每页 1 句引导 + 图示；细步进用 `fragment`（仅 `mode="slide"`）
- 图示用 `components/*`；适合演示的可做交互

## 阅读（VitePress）

- **`index.md`** 直接 `<ArticleSections mode="read" />`

## `presenter-evidence.md`（仅讲者）

- 与页序对应的代码抽象，`srcExclude` 不进站点

## 不要

- 不要在幻灯片路径使用 `index.md?raw` / `parse-slide-deck` / `data-markdown`
- 不要在图示组件里塞讲稿或点击交互
